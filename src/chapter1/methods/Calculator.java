package chapter1.methods;

public class Calculator {

    /*
     * int(반환자료형) sum(메서드이름) (int val1, int val2)(매개변수){
     *   int res = val1 + val2; (작업내용)
     *   return res;(반환값)
     * }
     * */

    int sum(int val1, int val2){
        int res = val1 + val2;
        return res;
    }
}
